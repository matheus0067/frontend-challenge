import React from 'react';
import { Meta } from '@storybook/react';
import { FilterByType } from './filterByType';

export default {
  title: 'Components/FilterByType',
  component: FilterByType,
} as Meta;

export const Default = () => <FilterByType />;